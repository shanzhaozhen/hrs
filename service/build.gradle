description = 'service'

dependencies {

    api(
            project(':repo')
    )

    implementation(
            'org.springframework.boot:spring-boot-starter-data-redis',
            "com.alibaba:easyexcel:${easyexcelVersion}",
    )

}



