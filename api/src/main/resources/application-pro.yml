server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://192.28.10.28:3306/hrsdb?useSSL=true&characterEncoding=utf8&serverTimezone=GMT%2B8
    username: root
    password: 123456
    #    driver-class-name: com.mysql.cj.jdbc.Driver
    #    schema: classpath:best.sql
  redis:
    ## Redis数据库索引（默认为0）
    database: 0
    ## Redis服务器地址
    host: 192.28.10.28
    ## Redis服务器连接端口
    port: 6379
    ## Redis服务器连接密码（默认为空）
    password: startlink

upload:
  realPath: /home/webFile/
  urlPath: /files/

logging:
  file:
    name: /logs/best-cloud/basic/basic.log
